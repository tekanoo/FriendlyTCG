rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles temporaires en mode test - À CHANGER EN PRODUCTION
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
